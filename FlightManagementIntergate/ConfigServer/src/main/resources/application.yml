spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rameez-Tamboli/MicroService-config
          clone-on-start: true

server:
  port: 8087
  